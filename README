This project is a variant of the card game Mau Mau. 

To run this program locally, you have to install parcel and a plug-in in the projects directory:

npm install parcel
npm install -D parcel-plugin-static-files-copy

npx parcel index.html
        
Server probably running at http://localhost:1234

Images that  are loaded at runtime have to be in the "./static" directory
